#!/usr/bin/bash

#Sliding window to get n-char long snips of a string (to use to grep for adapters)
# ${#string} outputs the length of the string
# ${string:position} gets a substring
#    ie ${string:0:20} gives first twenty chars of a string
# I should just write this in perl, it would not be hard at all. 

